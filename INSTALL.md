# Building Creature Pals

Clone this repo, then run 
```
./gradlew build
```
To build the mod. The built mod will be located in `build/libs/`.

To run the mod in a development environment, you can use:
```
./gradlew runClient
```
